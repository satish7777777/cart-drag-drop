import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images:{
    domains: ['images.unsplash.com', 'cdn.pixabay.com','picsum.photos','v.wpimg.pl', 'images.pexels.com']
  }
};

export default nextConfig;
